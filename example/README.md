# coheriq example

This directory contains the source code to a library (domain) with a
default implementation in Python, as well as a corresponding
acceleration engine written in C++, using nanobind.

For a demonstration of this code in use, see the [compiled engine
example notebook](../docs/demos/compiled_engine_example.ipynb).
